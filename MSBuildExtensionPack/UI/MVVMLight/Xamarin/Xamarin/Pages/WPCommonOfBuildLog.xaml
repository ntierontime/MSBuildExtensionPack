<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Xamarin.Pages.WPCommonOfBuildLog">

	<ListView x:Name="V_Common" ItemsSource="{Binding EntityCollectionDefault}">
		<ListView.ItemTemplate>
			<DataTemplate>
				<ViewCell>
					<StackLayout BackgroundColor="#eee"
                        Orientation="Vertical">
						<StackLayout Orientation="Horizontal">
							<Label Text="{Binding BuildId}" Style="{StaticResource Heading}"  />
							<Label Text="{Binding EventTime}" Style="{StaticResource SubHeading}"/>
							<Label Text="{Binding EventTime}" Style="{StaticResource SubHeading}"/>
							<Label Text="{Binding EventTime}" Style="{StaticResource SubHeading}"/>
							<Label Text="{Binding EventTime}" Style="{StaticResource SubHeading}"/>
							<Label Text="{Binding EventTime}" Style="{StaticResource SubHeading}"/>
						</StackLayout>
					</StackLayout>
				</ViewCell>
			</DataTemplate>
		</ListView.ItemTemplate>
	</ListView>
</ContentPage>